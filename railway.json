{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && bun install && bun run build"
  },
  "deploy": {
    "startCommand": "cd backend && bun run db:migrate && bun run start"
  }
}
